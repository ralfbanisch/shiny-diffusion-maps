# shiny-diffusion-maps

This is a shiny application. Requires R and R Studio. To launch, start RStudio
and type

shiny::runApp('path-to-app.R')

You may have to install some packages first, use e.g. install.packages('ggplot2').
